# CoCo-doc

A lightweight, self-hostable collaborative document editor. See
[ROADMAP.md](ROADMAP.md) for product direction and milestones.

## Running locally

```
npm install
npm start
```

Then open http://localhost:3000. Documents are persisted as JSON under
`data/` (gitignored).

## Running tests

```
npm test
```
